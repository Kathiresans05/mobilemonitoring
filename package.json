{
  "name": "enterprise-monitoring",
  "version": "1.0.0",
  "description": "Enterprise Mobile Monitoring System",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix dashboard",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
